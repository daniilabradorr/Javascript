import "./crud.js";
// Nota: JSONPlaceholder NO persiste cambios realmente.
// Para que el CRUD “se vea” reflejado en pantalla, el módulo crud.js
// mantiene internamente un array de usuarios que se actualiza al crear, editar o borrar.
// De este modo, aunque la API devuelva siempre la lista original,
// la interfaz muestra los cambios aplicados localmente.